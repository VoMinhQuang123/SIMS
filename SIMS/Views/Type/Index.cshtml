@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model List<SIMS.BDContext.Entity.Type>

@{
    ViewData["Title"] = "Type List";
    Layout = "~/Views/Shared/Layout_Admin/_layoutAdmin.cshtml";
}
<h2>Teacher List</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New Major</a>
</p>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Major</th>
            <th>Description</th>
            
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.TypeName</td>
                <td>@item.Description</td>
                

                <td>
                    <a asp-action="Details" asp-route-id="@item.TypeID" class="btn btn-sm btn-info">Details</a>
                    @* <a asp-action="Edit" asp-route-id="@item.CourseID" class="btn btn-sm btn-warning">Edit</a> *@
                    @* <a asp-action="Delete" asp-route-id="@item.CourseID" class="btn btn-sm btn-danger">Delete</a> *@
                </td>
            </tr>
        }
    </tbody>
</table>
